# Task: QA-002 - Run verification and manual E2E for draft invoice PDF flow

## Metadata
- Status: completed
- Estimate: 40m
- Depends on: BE-001, BE-002, FE-001, FE-002, FE-003, QA-001

## Files to Modify
- No source files

## Description
Chạy kiểm thử backend/frontend và manual E2E để xác nhận flow draft invoice, PDF, issue hoạt động từ UI tới worker/provider.

## Requirements
- Backend typecheck pass.
- Backend unit tests pass.
- Frontend typecheck pass.
- Frontend build pass.
- Manual E2E tạo draft từ order Pancake test.
- Worker xử lý được job create draft, không báo unknown job type.
- Invoice job detail hiển thị status mới sau check status.
- View/download PDF hoạt động hoặc hiển thị lỗi rõ nếu SePay chưa hỗ trợ draft PDF.
- Issue flow hoạt động tới trạng thái issued nếu provider credential/response cho phép.

## Verification
```bash
cd backend
npm run typecheck
npm test
```

```bash
cd frontend
npm run typecheck
npm run build
```

Manual E2E:
```text
/login/dev
/invoices/preview?orderId=42
Use shop id 259547b8-f507-48d6-aa52-5b43313cc203
Preview invoice
Create draft job
Run backend worker
Open /jobs?invoiceJobId=<id>
Check status until draft_created
View PDF
Download PDF
Issue
Check status until issued
Download final PDF
```

## Notes
- Nếu SePay sandbox không trả PDF cho draft, ghi nhận rõ response/lỗi để quyết định backend normalize hoặc UI message ở vòng sau.
- Không sửa source files trong task này trừ khi tạo bug-fix task mới được approve.
- Completed as baseline. Full sandbox E2E after Shichida port is tracked in `../sepay-shichida-port/`.

---
**⚠️ KHÔNG VIẾT CODE Ở ĐÂY** - Code sẽ được implement bởi Executor agent
