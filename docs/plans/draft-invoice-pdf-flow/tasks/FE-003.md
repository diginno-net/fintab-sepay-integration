# Task: FE-003 - Fix links and redirects into invoice job detail

## Metadata
- Status: completed
- Estimate: 30m
- Depends on: FE-001

## Files to Modify
- `frontend/features/invoice-preview/invoice-preview-form.tsx` (MODIFY)
- `frontend/app/(platform)/invoices/page.tsx` (MODIFY)

## Description
Đảm bảo tất cả luồng từ preview và invoices list đều mở đúng invoice job detail bằng query `invoiceJobId`.

## Requirements
- Sau khi tạo draft job thành công, preview form redirect tới `/jobs?invoiceJobId=<invoice_jobs.id>`.
- Invoices page link `Open job` phải trỏ tới invoice job detail, không dùng background job id.
- Invoices page nên hiển thị đủ thông tin cơ bản để user biết job nào cần mở: order id, status, reference code, invoice number nếu có.
- Không thay đổi behavior preview payload.

## Verification
```bash
cd frontend
npm run typecheck
```

Manual:
```text
/invoices/preview
Create draft job
Confirm redirect to /jobs?invoiceJobId=<id>
/invoices
Click Open job
```

## Notes
- Nếu API trả `{ invoiceJob, backgroundJobId }`, redirect phải dùng `invoiceJob.id`.
- Completed as baseline. Provider/template config links are tracked in `../sepay-shichida-port/`.

---
**⚠️ KHÔNG VIẾT CODE Ở ĐÂY** - Code sẽ được implement bởi Executor agent
