# Task: QA-001 - Update frontend smoke tests for JobActions

## Metadata
- Status: completed
- Estimate: 35m
- Depends on: FE-002

## Files to Modify
- `frontend/tests/job-actions-smoke.test.tsx` (MODIFY)

## Description
Cập nhật smoke tests để phản ánh action buttons mới cho invoice job status, đặc biệt là view/download PDF ở trạng thái draft và issued.

## Requirements
- Test trạng thái failed/timeout hiển thị Retry.
- Test trạng thái `draft_created` hiển thị Issue, View PDF và Download PDF.
- Test trạng thái `issued` hiển thị View PDF và Download PDF, không hiển thị Issue.
- Test trạng thái queued/polling hiển thị Check status.
- Không cần gọi network thật; mock API/fetch theo pattern test hiện có.

## Verification
```bash
cd frontend
npm test -- job-actions-smoke
```

Nếu project không có script test frontend tương ứng, dùng command test hiện có trong package frontend.

## Notes
- Không sửa component trong task này.
- Completed as baseline. Additional provider picker/download tests are tracked in `../sepay-shichida-port/`.

---
**⚠️ KHÔNG VIẾT CODE Ở ĐÂY** - Code sẽ được implement bởi Executor agent
