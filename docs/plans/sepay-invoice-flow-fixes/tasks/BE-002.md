# BE-002 - Preserve worker error details

Add error serialization in `backend/src/queue-worker.ts`.

Persist fields:
- `jobId`
- `name`
- `code`
- `message`
- `statusCode`
- `details`

Apply to both `background_jobs.last_error_json` and `invoice_jobs.error_json`.
