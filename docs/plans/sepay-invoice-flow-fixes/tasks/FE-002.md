# FE-002 - Fix invoice job error display

Files:
- `frontend/features/invoices/invoice-job-detail.tsx`
- `frontend/features/invoices/invoice-error-messages.ts`

Requirements:
- Use `error_json` as primary failure.
- Do not use `mapping_warnings_json` as primary failure.
- Render warnings separately.
- Link SePay config actions to the current shop.
