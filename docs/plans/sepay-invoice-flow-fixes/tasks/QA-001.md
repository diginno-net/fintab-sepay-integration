# QA-001 - Verification

Run:

```bash
npm --prefix backend run typecheck
npm --prefix backend test
npm --prefix frontend run typecheck
```

Manual check:
- Bad credentials show a SePay auth error.
- After valid credentials are entered, retry draft -> issue -> PDF.
