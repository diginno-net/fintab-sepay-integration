# Task UI-004: Verification

## Scope
- Run typecheck, test, and build.
- Fix UI-related compile or test regressions.

## Acceptance Criteria
- `npm --prefix frontend run typecheck` passes.
- `npm --prefix frontend test` passes.
- `npm --prefix frontend run build` passes or any failure is documented with root cause.
