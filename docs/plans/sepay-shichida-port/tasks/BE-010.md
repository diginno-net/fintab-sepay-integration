# Task: BE-010 - Short-poll create draft

## Metadata
- Status: pending
- Estimate: 50m
- Depends on: BE-009

## Files to Modify
- `backend/src/modules/jobs/job-handlers.ts` (MODIFY)

## Description
After create invoice request, poll create status briefly to obtain `reference_code` when available.

## Requirements
- Extract fields from create response.
- If tracking exists, poll 3-5 attempts with short delay.
- Set `draft_created` when usable reference code exists.
- Keep `draft_create_polling` when still pending.

## Verification
Worker should reduce stuck `draft_create_polling`/manual check states.

---
**⚠️ KHÔNG VIẾT CODE Ở ĐÂY** - Code sẽ được implement bởi Executor agent
