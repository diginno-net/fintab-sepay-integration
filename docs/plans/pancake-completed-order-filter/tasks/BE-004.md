# BE-004 Integrate Invoice Read Service

Update `invoice-order-read.service.ts`.

Requirements:

- Use the central Pancake filter builder for live Pancake queries.
- Support `completedOnly`, `completedDays`, `filterStatus`, `updateStatus`, `startDateTime`, `endDateTime`, `optionSort`.
- Preserve `paidOnly` as local payment filter.
- Preserve invoice status filtering.
