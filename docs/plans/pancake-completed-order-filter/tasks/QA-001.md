# QA-001 Verification

Run:

```bash
npm --prefix backend test
npm --prefix backend run typecheck
```

Manual checks:

- Completed query has `filter_status[]=3`.
- Sync with completed-only enqueues successfully.
- Invoice list loads with completed-only filter.
- Paid guard remains enforced before invoice issue.
