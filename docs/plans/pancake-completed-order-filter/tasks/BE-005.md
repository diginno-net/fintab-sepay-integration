# BE-005 Integrate Order Sync

Update:

- `pancake-order-sync.service.ts`
- `pancake.routes.ts`
- `queue-worker.ts`

Requirements:

- Sync endpoint accepts completed-order filter options.
- Job payload carries filter options.
- Worker passes filter options into `processPancakeOrderFullSyncJob`.
- Full sync with no filters remains unchanged.
