# FE-001 Invoice UI Filters

Update invoice UI filtering to expose completed-order filter.

Requirements:

- Add `completedOnly` state.
- Add `completedDays` state.
- Pass both params to `/v1/invoice-orders`.
- Keep existing `paidOnly` as a separate checkbox.
