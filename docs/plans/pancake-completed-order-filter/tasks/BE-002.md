# BE-002 Add Filter Builder Tests

Add `backend/src/modules/pancake/pancake-order-filter.test.ts`.

Cover:

- Completed order query defaults to Pancake `status=3`.
- `completedDays` creates Unix second date range.
- Explicit `filterStatus` supports multiple statuses.
- Legacy scalar `status` still works.
- Legacy `date_from`/`date_to` still works.
- Undefined values are omitted.
