# Task: QA-004 - Manual E2E draft guards

## Metadata
- Status: completed
- Estimate: 45m
- Depends on: QA-003

## Files to Modify
- None

## Description
Manually verify draft outdated and company mismatch guard behavior.

## Requirements
- Save valid invoice request.
- Create draft.
- Edit invoice request fields that affect invoice buyer hash.
- Verify draft-status shows outdated/recreate warning.
- Attempt issue and verify it is blocked.
- Test company request validation with missing MST/name/address.

## Verification
```text
Manual browser test plus API checks on localhost.
```

## Result
- Preview for order `40` succeeded with saved buyer data.
- Draft job was created for order `40`.
- After editing invoice request address/contact, draft-status returned:
  - `outdated=true`
  - `requiresDraftRecreate=true`
  - expected outdated draft message
- Issue API was tested by temporarily setting the test job to `draft_created`.
- Issue was blocked with `VALIDATION_ERROR` and `requiresDraftRecreate=true`.
- Test invoice job and background job were then marked `cancelled` to avoid accidental SePay processing by a worker.

---
**⚠️ KHÔNG VIẾT CODE Ở ĐÂY** - Code sẽ được implement bởi Executor agent
