# Task: INT-002 - Add production issue guard

## Metadata
- Status: pending
- Estimate: 1h
- Depends on: SEC-001

## Files to Modify
- `backend/src/modules/invoices/invoice-job.service.ts` (MODIFY if guard belongs before enqueue)
- `backend/src/modules/invoices/invoice-workflow.service.ts` (MODIFY if applicable)
- `backend/src/modules/sepay/sepay.service.ts` (MODIFY if config validation belongs there)
- `backend/src/modules/invoices/invoice-state-machine.ts` (MODIFY only if state guard is needed)

## Description
Add explicit guardrails before production invoice issue operations.

## Requirements
- Verify shop has SePay config before issue.
- Verify credentials exist.
- Verify `provider_account_id`, `template_code`, and `invoice_series` exist.
- Reject sandbox client id when config env is production.
- Preserve existing draft outdated check.
- Preserve valid invoice state requirements.
- Return actionable validation errors with safe details.

## Verification
```bash
npm --prefix backend test -- invoice
npm --prefix backend test -- sepay
npm --prefix backend run typecheck
```

## Notes
- This guard protects production issue. It should not block create draft unless the missing config also makes draft invalid.

---
**⚠️ KHÔNG VIẾT CODE Ở ĐÂY** - Code sẽ được implement bởi Executor agent
