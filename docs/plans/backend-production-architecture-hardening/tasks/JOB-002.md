# Task: JOB-002 - Harden job claim locking

## Metadata
- Status: pending
- Estimate: 1h
- Depends on: JOB-001

## Files to Modify
- `backend/src/queue-worker.ts` (MODIFY)

## Description
Strengthen job claiming so jobs cannot remain locked forever and concurrent workers do not process the same job.

## Requirements
- Claim only queued jobs whose `run_after` is due.
- Treat expired locks as claimable if `locked_until` is in the past.
- Set `locked_at`, `locked_until`, and `locked_by` on claim.
- Use database locking semantics already present in the project.
- Log claimed job with worker id, job id, type, tenant id, and attempt number.
- Do not change invoice business handlers in this task.

## Verification
```bash
npm --prefix backend run typecheck
```

## Notes
- Preserve existing job types and handler routing.
- If schema already has `locked_until`, use it. If missing, document blocker before implementing schema changes.

---
**⚠️ KHÔNG VIẾT CODE Ở ĐÂY** - Code sẽ được implement bởi Executor agent
