# Task: SEC-003 - Add security tests

## Metadata
- Status: pending
- Estimate: 1h
- Depends on: SEC-002

## Files to Modify
- `backend/tests/security-origin-guard.test.ts` (CREATE)

## Description
Add tests for mutating request origin protection.

## Requirements
- Verify allowed origin can call a representative mutating route.
- Verify disallowed origin is rejected.
- Verify missing/unsafe origin behavior matches intended server-side/browser policy.
- Verify `GET` route is not blocked.
- Verify `/v1/webhooks/pancake` is exempt from origin guard but still requires webhook secret handling.

## Verification
```bash
npm --prefix backend test -- security-origin-guard
npm --prefix backend run typecheck
```

## Notes
- Avoid relying on real external Pancake/SePay calls.

---
**⚠️ KHÔNG VIẾT CODE Ở ĐÂY** - Code sẽ được implement bởi Executor agent
