# Task: SEC-001 - Strengthen production env validation

## Metadata
- Status: pending
- Estimate: 45m
- Depends on: ARCH-001

## Files to Modify
- `backend/src/config/env.ts` (MODIFY)
- `.env.example` (MODIFY)

## Description
Ensure production cannot start with insecure default or missing environment configuration.

## Requirements
- Fail fast when `NODE_ENV=production` and `SESSION_SECRET` is default, weak, or missing.
- Fail fast when `NODE_ENV=production` and `ENCRYPTION_MASTER_KEY` is default, weak, or missing.
- Fail fast when production `CORS_ORIGIN` is wildcard, empty, or unsafe.
- Ensure `DATABASE_URL` is required.
- Document expected production env variables in `.env.example`.
- Include `NEXT_PUBLIC_API_BASE_URL` in env documentation if frontend depends on it.

## Verification
```bash
npm --prefix backend run typecheck
```

## Notes
- Do not print secret values in validation errors or logs.

---
**⚠️ KHÔNG VIẾT CODE Ở ĐÂY** - Code sẽ được implement bởi Executor agent
