# Task: JOB-004 - Add worker reliability tests

## Metadata
- Status: pending
- Estimate: 1h
- Depends on: JOB-003

## Files to Modify
- `backend/tests/worker-retry.test.ts` (CREATE)

## Description
Add tests for background job retry, backoff, stale lock handling, and terminal failure behavior.

## Requirements
- Verify failed job with attempts remaining is requeued with future `run_after`.
- Verify attempts are incremented.
- Verify exhausted job is terminal failed/dead-letter equivalent.
- Verify stale locked job can be claimed after `locked_until` expires.
- Verify successful job clears lock and becomes succeeded.

## Verification
```bash
npm --prefix backend test -- worker-retry
npm --prefix backend run typecheck
```

## Notes
- Use existing test patterns/helpers where possible.
- Keep tests deterministic; avoid real long waits.

---
**⚠️ KHÔNG VIẾT CODE Ở ĐÂY** - Code sẽ được implement bởi Executor agent
