# Task: SEC-002 - Add origin guard for mutating requests

## Metadata
- Status: pending
- Estimate: 1h
- Depends on: SEC-001

## Files to Modify
- `backend/src/shared/security/origin-guard.ts` (CREATE)
- `backend/src/app.ts` (MODIFY)

## Description
Add CSRF-style origin protection for cookie-authenticated mutating API requests.

## Requirements
- Apply guard to `POST`, `PUT`, `PATCH`, and `DELETE` requests.
- Allow only configured frontend origins.
- Exempt inbound Pancake webhook route `/v1/webhooks/pancake`.
- Keep webhook secret verification as the boundary for exempt webhook route.
- Return a consistent error envelope when origin is forbidden.
- Avoid blocking safe methods such as `GET` and `HEAD`.

## Verification
```bash
npm --prefix backend run typecheck
```

## Notes
- If Next.js server-side API calls do not include Origin, support a safe same-service/server-side condition without weakening browser protection.

---
**⚠️ KHÔNG VIẾT CODE Ở ĐÂY** - Code sẽ được implement bởi Executor agent
