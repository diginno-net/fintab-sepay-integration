# Task: OPS-001 - Add DB readiness endpoint

## Metadata
- Status: pending
- Estimate: 45m
- Depends on: ARCH-001

## Files to Modify
- `backend/src/app.ts` (MODIFY)
- `backend/src/shared/persistence/db.ts` (MODIFY if needed)

## Description
Add a readiness endpoint that verifies the API can reach PostgreSQL.

## Requirements
- Keep lightweight liveness endpoint behavior for `/v1/health`.
- Add `/v1/ready` or equivalent readiness endpoint.
- Readiness should run a minimal DB check.
- Return success when DB is reachable.
- Return 503 with safe error envelope when DB is unavailable.
- Do not expose database credentials or connection details.

## Verification
```bash
npm --prefix backend run typecheck
```

## Notes
- Endpoint should be safe for load balancer/readiness probes.

---
**⚠️ KHÔNG VIẾT CODE Ở ĐÂY** - Code sẽ được implement bởi Executor agent
