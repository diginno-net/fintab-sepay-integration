# Task: JOB-003 - Implement retry/backoff/dead-letter

## Metadata
- Status: pending
- Estimate: 1h
- Depends on: JOB-002

## Files to Modify
- `backend/src/queue-worker.ts` (MODIFY)
- `backend/src/shared/queue/job-queue.ts` (MODIFY)

## Description
Implement production-grade failure handling for background jobs.

## Requirements
- Increment attempts when a job processing attempt fails.
- Retry transient failures when attempts remain.
- Set `run_after` according to retry policy backoff.
- Clear lock fields when scheduling retry or final failure.
- Mark job terminal failed/dead-letter when attempts are exhausted.
- Preserve invoice job status/error updates for invoice-related jobs.
- Log retry scheduled and terminal failure events.

## Verification
```bash
npm --prefix backend run typecheck
```

## Notes
- Do not introduce pg-boss in this task.
- Keep behavior compatible with existing manual retry endpoint.

---
**⚠️ KHÔNG VIẾT CODE Ở ĐÂY** - Code sẽ được implement bởi Executor agent
