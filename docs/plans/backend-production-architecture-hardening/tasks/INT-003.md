# Task: INT-003 - Add integration guard tests

## Metadata
- Status: pending
- Estimate: 1h
- Depends on: INT-001, INT-002

## Files to Modify
- Backend tests for SePay/invoice integration guardrails (CREATE/MODIFY)

## Description
Add tests proving SePay parsing and production issue guardrails work.

## Requirements
- Test normalized parser handles expected success/pending/failure response variants.
- Test issue is rejected when SePay config is missing.
- Test issue is rejected when provider account/template/series is missing.
- Test production config rejects sandbox-style client id.
- Test valid configured draft can proceed to issue enqueue path without external provider calls.

## Verification
```bash
npm --prefix backend test -- sepay
npm --prefix backend test -- invoice
npm --prefix backend run typecheck
```

## Notes
- Use fake or database fixtures, not real SePay API.

---
**⚠️ KHÔNG VIẾT CODE Ở ĐÂY** - Code sẽ được implement bởi Executor agent
