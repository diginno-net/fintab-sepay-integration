# FE-008 - Add user-shop access management UI

## Metadata
- Status: pending
- Estimate: 1h 30m
- Depends on: BE-013

## Files to Modify
- `frontend/app/(platform)/settings/shop-access/page.tsx` (CREATE)
- `frontend/features/shop-access/shop-access-client.ts` (CREATE)
- `frontend/features/shop-access/shop-access-table.tsx` (CREATE)
- `frontend/components/navigation/main-nav.tsx`

## Description
Add Settings UI for assigning users to shops.

## Requirements
- Show users, roles, assigned shops, and default shop.
- Allow admin to edit assigned shops.
- Allow admin to choose default shop.
- Hide or disable UI for users without `shop_access:write`.

## Verification
```bash
npm --prefix frontend run typecheck
```
