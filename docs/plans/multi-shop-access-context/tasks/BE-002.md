# BE-002 Identity Session Integration

Update:

- `auth-context.ts`
- `identity.routes.ts`

Requirements:

- `AuthUser` includes `sessionId` and `currentShopId`.
- `/v1/me` returns accessible shops only.
- `PUT /v1/me/current-shop` validates access and persists the session current shop.
