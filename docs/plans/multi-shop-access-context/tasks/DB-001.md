# DB-001 User Shop Access Migration

Add `backend/db/migrations/006_user_shop_access.sql`.

Requirements:

- Create `user_shop_access`.
- Add `sessions.current_tenant_shop_id`.
- Backfill existing memberships to all tenant shops for safe migration.
- Mark the first shop per user as default.
