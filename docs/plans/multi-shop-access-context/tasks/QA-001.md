# QA-001 Verification

Run:

```bash
npm --prefix backend test
npm --prefix backend run typecheck
npm --prefix frontend run typecheck
```

Manual checks:

- `/v1/me` only returns allowed shops.
- Changing to inaccessible shop returns 403.
- Navigation preserves selected shop.
