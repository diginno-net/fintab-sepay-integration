# Task: PAN-002 - Implement Pancake order/product endpoints

## Metadata
- Status: pending
- Estimate: 60m
- Depends on: PAN-001, RBAC-001

## Files to Modify
- `backend/src/modules/pancake/pancake.routes.*` CREATE
- `backend/src/modules/pancake/pancake-status-policy.*` CREATE

## Description
Expose backend endpoints for Pancake orders, order detail and products/variations by shop.

## Requirements
- `GET /v1/pancake/shops/:shopId/orders`.
- `GET /v1/pancake/shops/:shopId/orders/:orderId`.
- `GET /v1/pancake/shops/:shopId/products`.
- Support filters: status, search, date range, updateStatus where practical.
- Include order status policy helper.
- Detect `einvoices` or `invoice_info_list` in order detail.

## Verification
```bash
npm --prefix backend test -- pancake-routes
```

## Notes
- Every route must validate current tenant can access `shopId`.
