# Task: INV-002 - Implement invoice preview endpoint

## Metadata
- Status: pending
- Estimate: 75m
- Depends on: INV-001, TAX-002

## Files to Modify
- `backend/src/modules/invoices/invoice.routes.*` CREATE
- `backend/src/modules/invoices/invoice.service.*` CREATE
- `backend/src/modules/invoices/invoice-snapshot.service.*` CREATE

## Description
Implement preview endpoint that returns SePay payload preview and stores immutable source order snapshot when appropriate.

## Requirements
- Validate RBAC `invoice:preview`.
- Validate shop ownership.
- Fetch Pancake order detail.
- Detect existing Pancake invoice info.
- Create preview payload with warnings.
- Include tax resolution metadata per line: product_profile, tenant_profile, shop_default or manual_override.
- If shop policy is `block`, return validation error when item lacks tax profile.
- Prepare snapshot for later create draft flow.

## Verification
```bash
npm --prefix backend test -- invoice-preview
```

## Notes
- Preview must not call SePay create or issue.
