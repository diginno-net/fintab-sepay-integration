# Task: JOB-001 - Implement background job infrastructure

## Metadata
- Status: historical-superseded
- Estimate: 60m
- Depends on: DB-001

## Files to Modify
- `backend/src/shared/queue/*` CREATE
- `backend/src/modules/jobs/*` CREATE

## Description
Original plan expected pg-boss. Current implemented architecture uses the custom PostgreSQL `background_jobs` queue and `backend/src/queue-worker.ts`; pg-boss is deferred.

## Requirements
- Use the custom PostgreSQL `background_jobs` queue for the current runtime.
- Support enqueue, status, retry policy and worker bootstrap.
- Track poll attempts separately from execution attempts.
- Include stale lock or equivalent recovery policy.
- Include graceful shutdown behavior.

## Verification
```bash
npm --prefix backend test -- jobs
```

## Notes
- This task does not implement invoice-specific handlers.
- Superseded by the production hardening decision documented in `docs/backend-architecture.md` and `docs/plans/backend-production-architecture-hardening/plan.md`.
