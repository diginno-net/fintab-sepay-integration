# Task: FE-002 - Implement frontend design system

## Metadata
- Status: pending
- Estimate: 60m
- Depends on: FE-001

## Files to Modify
- `frontend/components/forms/*` CREATE
- `frontend/components/status/*` CREATE
- `frontend/components/skeletons/*` CREATE
- `frontend/components/empty-states/*` CREATE
- `frontend/components/error-states/*` CREATE
- `frontend/styles/*` MODIFY/CREATE

## Description
Implement UI primitives and visual system according to the high-agency frontend rules.

## Requirements
- Use desaturated emerald accent.
- Use premium sans font stack, not Inter.
- Use `@phosphor-icons/react` after dependency verification.
- Provide button, input, select, table, badge, skeleton, empty and error primitives.
- Labels above inputs and errors below inputs.
- Tactile active states for buttons.

## Verification
```bash
npm --prefix frontend run build
```

## Notes
- Do not use neon, purple-blue AI gradients, pure black or emoji.
