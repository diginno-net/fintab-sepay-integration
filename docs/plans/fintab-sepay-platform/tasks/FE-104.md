# Task: FE-104 - Build product catalog screens

## Metadata
- Status: pending
- Estimate: 75m
- Depends on: FE-004, PROD-002, TAX-001

## Files to Modify
- `frontend/app/products/*` CREATE/MODIFY
- `frontend/app/products/[productId]/tax/*` CREATE
- `frontend/features/product-catalog/*` CREATE
- `frontend/features/tax-mapping/*` CREATE/MODIFY

## Description
Build product catalog list, lookup, Fintab Excel import UI and product tax profile UI.

## Requirements
- Upload import form.
- Show import result counts.
- Product table filters by group, status and source.
- Lookup by product code/barcode.
- Product table shows tax_rate, invoice unit and tax profile status.
- Product detail supports editing tax_rate, invoice_line_type and invoice_unit.
- Bulk update tax_rate by product group.
- Empty and error states.

## Verification
```bash
npm --prefix frontend run build
```

## Notes
- Do not block other screens if catalog is empty.
