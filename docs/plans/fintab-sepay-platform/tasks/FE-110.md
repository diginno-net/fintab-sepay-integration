# Task: FE-110 - Build onboarding flow for new tenants

## Metadata
- Status: pending
- Estimate: 60m
- Depends on: FE-104

## Files to Modify
- `frontend/app/(auth)/onboarding/*` CREATE
- `frontend/features/shop-management/*` MODIFY
- `frontend/features/pancake-config/*` MODIFY
- `frontend/features/sepay-config/*` MODIFY
- `frontend/features/tax-mapping/*` MODIFY

## Description
Build first-time onboarding flow for a new tenant to create the first shop, configure Pancake, configure SePay, and set default tax mapping.

## Requirements
- Step 1: confirm tenant/company info.
- Step 2: create first shop with Pancake shop_id and shop_name.
- Step 3: enter Pancake api_key and test connection.
- Step 4: enter SePay credentials and test connection.
- Step 5: configure provider account, template, invoice series and default payment method.
- Step 6: configure default tax rate and unknown product policy.
- Redirect to dashboard after completion.
- Each step has loading, validation and error states.

## Verification
```bash
npm --prefix frontend run build
```

## Notes
- Do not render raw secrets after save.
- Onboarding should be resumable if a step succeeds and later steps fail.
