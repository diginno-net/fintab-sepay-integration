# Task: PAN-001 - Implement Pancake API client

## Metadata
- Status: pending
- Estimate: 60m
- Depends on: INT-002

## Files to Modify
- `backend/src/modules/pancake/pancake-client.*` CREATE
- `backend/src/modules/pancake/pancake.service.*` CREATE

## Description
Implement Pancake POS API client using query parameter `api_key` and per-shop credentials.

## Requirements
- Base URL: `https://pos.pages.fm/api/v1` by default.
- Auth uses query parameter `api_key`.
- Never use Authorization Bearer for Pancake POS API.
- Support test connection.
- Ensure logs never include full URL with API key.

## Verification
```bash
npm --prefix backend test -- pancake-client
```

## Notes
- Use OpenAPI docs as source of truth for auth.
